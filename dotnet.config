# Route `dotnet test` to the Microsoft Testing Platform runner so it discovers
# xUnit v3 (MTP) tests instead of using the legacy VSTest path.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
