#!/bin/sh
# shellcheck source=/dev/null  # husky.sh is generated in .husky/_/ at install time
. "$(dirname "$0")/_/husky.sh"

## husky task runner examples -------------------
## Note : for local installation use 'dotnet' prefix. e.g. 'dotnet husky'

## run all tasks
#husky run

### run all tasks with group: 'group-name'
#husky run --group group-name

## run task with name: 'task-name'
#husky run --name task-name

## pass hook arguments to task
#husky run --args "$1" "$2"

## or put your custom commands -------------------
#echo 'Husky.Net is awesome!'

# Vendored from paragon-stats/github-actions@8e164c6 (v2.2.1); the commitlint CI
# job runs the same script, and its drift step keeps this copy byte-identical to
# the tagged upstream. Invoked via `bash` because a core.filemode=false (Windows)
# clone records the file 100644, which would exit 126 if run by path. `set -f` so
# the script's own path handling never expands *, ?, or [ ] against the tree.
set -f
MESSAGE_FILE="$1" CHANGED_PATHS="$(git diff --cached --name-only)" bash scripts/git/check-commit-message.sh
