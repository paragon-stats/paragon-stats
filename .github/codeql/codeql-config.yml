name: paragon-stats CodeQL config

# Build artifacts are generated, not authored — don't scan them. xUnit/MSBuild emit
# C# into obj/ (e.g. the test entry point, AssemblyInfo, GlobalUsings), which would
# otherwise raise maintainability alerts on code we don't own or edit.
paths-ignore:
  - "**/obj/**"
  - "**/bin/**"
